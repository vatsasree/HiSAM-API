what I was doing?

to create a user and test using scripts/create_user.py
and use the api endpoint (docs) to authorize and process a request
    get the uuid
    check status using uuid - in process.py change status after say 10 sec and see if /status is changing
        for now in a dictionary or something
add database to endpoints
test multiple requests with celery and redis - are they working
